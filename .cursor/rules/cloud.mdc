---
description: Cloud agent, autonomous delivery
alwaysApply: true
metadata:
  environments: cloud
---

**Setup:** follow **`README.md`**. Below is only what differs in the cloud environment.

## Environment

- Secrets usually come from the Cursor dashboard.
- If env vars fail, notify the user and stop.
- **Do not** create `.env` files in the cloud.
- All vars from `.env.example` must be set in the dashboard when needed.
- Do not run `--apply` against production unless the task explicitly says so.
